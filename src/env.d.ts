interface ImportMetaEnv {
  /** The GoatCounter site code (or a full https URL ending in /count). Unset, as everywhere but the Pages job, nothing is counted. */
  readonly VITE_GOATCOUNTER?: string
}
